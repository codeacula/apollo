# Docker build context ignore file
# Keeps .git for dev container (needed for git operations inside container)
# Excludes build artifacts and IDE files to keep context small

**/.classpath
**/.dockerignore
**/.env
**/.gitignore
**/.project
**/.settings
**/.toolstarget
**/.vs
**/.vscode
**/*.*proj.user
**/*.dbmdl
**/*.jfm
**/bin
**/obj
**/node_modules
**/npm-debug.log
**/charts
**/secrets.dev.yaml
**/values.dev.yaml
**/TestResults

# Keep .git (needed for dev container git operations)
# Keep docker/ (needed for build)
# Keep compose files (excluded from image via Dockerfile, not here)
