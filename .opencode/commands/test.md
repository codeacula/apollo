---
description: Run all tests in the solution
agent: build
---
Run `dotnet test Apollo.sln --verbosity normal` and report results.

If any tests fail:
1. Show the failing test names and error messages
2. Analyze the root cause of each failure
3. Suggest specific fixes
